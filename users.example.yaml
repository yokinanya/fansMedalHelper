USERS:
    - access_key: XXXXXX # 注意冒号后的空格 否则会读取失败 英文冒号
      white_uid: 0 # 白名单用户ID, 可以是多个用户ID, 以逗号分隔,填写后只会打卡这些用户,黑名单失效，不用就填0
      banned_uid: 0 # 黑名单UID 同上,填了后将不会打卡，点赞，分享 用英文逗号分隔 不填则不限制,两个都填0则不限制,打卡所有直播间
      # 投币配置（用户级别，可选）
      coin_remain: 0 # 保留硬币数,默认0个,即投完为止
      coin_uid: 0 # 投币指定UP主，可以是单个UID或多个UID（用逗号分隔），如：12345 或 12345,67890,11111。设置为0则不投币
      coin_max: 0 # 每日投币上限,设置为0则不设置上限
      coin_max_per_uid: 0 # 每个UP主每日投币上限，设置为0则不限制单个UP主投币数
    - access_key:
      white_uid: 0
      banned_uid: 0
      # 第二个用户示例：启用投币
      coin_uid: 123456,789012 # 投币给这两个UP主
      coin_max: 6 # 每日最多投6个币
      coin_max_per_uid: 0 # 每个UP主每日投币上限，设置为0则不限制单个UP主投币数
    # 注意对齐
    # 多用户以上格式添加
    # 井号后为注释 井号前后必须有空格！井号前后必须有空格！井号前后必须有空格！
    # 冒号后面也要有空格！冒号前面也要有空格！冒号前面也要有空格！
    # 英文冒号,英文逗号！英文逗号！英文逗号！
CRON: # 0 0 * * *
# 这里是 cron 表达式, 第一个参数是分钟, 第二个参数是小时
# 例如每天凌晨0点0分执行一次为 0 0 * * *
# 如果不填,则不使用内置定时器,填写正确后要保持该进程一直运行

SENDKEY: # Server酱微信推送 可选 获取地址：https://sct.ftqq.com/
MOREPUSH: # 更多种类的推送 详细配置见文档

PROXY: # 推送代理地址,如：http://1.2.3.4:5678 支持 http/socks4/socks5 不支持 https 不用代理的不用填
#########以下为自定义配置#########

ASYNC: 0 # 异步执行,默认同步执行,设置为1则异步执行

LIKE_CD: 3 # 点赞间隔时间,单位秒,默认3秒,设置为0则不点赞

DANMAKU_NUM: 10 # 弹幕发送次数,默认10次, 设置为0则不发弹幕打卡

DANMAKU_CD: 6 # 弹幕间隔时间,单位秒,默认6秒,设置为0则不发弹幕打卡,只能同步打卡

WATCHINGLIVE: 25 # 每日每直播间观看时长，单位 min ，设置为0则关闭, 默认 25 分钟

WEARMEDAL: 0 # 是否弹幕打卡时自动带上当前房间的粉丝牌，避免房间有粉丝牌等级禁言，默认关闭，设置为1则开启

SIGNINGROUP: 2 # 应援团签到CD时间,单位秒,默认2秒,设置为0则不签到
# 说明：
# 本项目中的异步执行指的是：同时点赞或者分享所有直播间，速度非常快，但缺点就是可能会被B站吞掉亲密度，所以建议粉丝牌较少的用户开启异步执行
# 粉丝牌数大于30的用户建议使用同步，会更加稳定。缺点就是速度比较慢，但是可以设置点赞和分享的CD时间，避免被B站吞掉亲密度
# 多用户之间依然是异步，不受配置影响

# 投币功能说明：
# - 投币配置现在支持用户级别设置，每个用户可以有独立的投币配置
# - 在USERS下的每个用户可以单独配置coin_remain、coin_uid、coin_max、coin_max_per_uid
# - 如果用户未配置某项，将使用全局默认值
# - coin_uid设置为0则不投币，设置具体UP主UID则启用投币功能

# 修改内容 by @yokinanya：
# 1. 修改点赞和发送弹幕逻辑，只在下播时发送弹幕，直播时点赞，避免对主播造成困扰
# 2. 投币功能支持用户级别配置，每个用户可以独立设置投币目标